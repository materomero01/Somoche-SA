var express = require('express');
var router = express.Router();
var ctrlNavigation = require('../controllers/ctrlNavigation.js');

/* GETs navigation */
// router.get('/', ctrlNavigation.home);

/* POSTs navigation */

module.exports = router;